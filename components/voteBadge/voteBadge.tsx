import styled from "../../typed-components";

const Container = styled.div``;

interface IProps {
  icon: any;
  number: number;
}

const VoteBadge: React.SFC<IProps> = () => <Container />;

export default VoteBadge;
